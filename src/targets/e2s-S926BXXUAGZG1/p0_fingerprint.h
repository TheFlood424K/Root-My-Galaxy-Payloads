/* e2s-S926BXXUAGZG1 shares the android16-6.6 GKI kernel with e1s.
 * Re-use the fingerprint table verbatim; the oracle probes the same
 * physical-page patterns regardless of which S25 variant is running. */
#include "../e1s-S921BXXSAGZG1/p0_fingerprint.h"
